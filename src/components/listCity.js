'use strict';

var React = require('react');

var ListCity = React.createClass({
    render: function() {
        return (
            <ul>
                
            </ul>
        );
    }
});

module.exports = ListCity;